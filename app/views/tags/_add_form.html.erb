<% form_remote_for taggable, :url => add_tag_posts_path(taggable), :html => {:id => "add_tag_#{taggable.id}", :class => 'if_admin', :style => 'display:none' } do |f| -%>
  <%= text_field_tag :new_tags %>
  <%= f.submit 'Add Tags' %>
<%- end -%>